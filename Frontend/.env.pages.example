# Copy to .env.production.local when building for GitHub Pages
# Or set as GitHub repository variable VITE_API_BASE (Settings → Secrets and variables → Actions → Variables)

# Backend API URL (staging or production)
VITE_API_BASE=https://taskagent-api-staging.nicemeadow-03f2da8a.eastus.azurecontainerapps.io

# Socket.IO realtime server URL (optional; board updates in real time when set)
# VITE_REALTIME_URL=https://your-realtime-server.example.com
