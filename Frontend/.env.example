# Copy to .env or .env.local so the UI uses the real API (not mock mode).
# Required for login (JWT) and all API calls.

# Backend API URL. Set so the UI is not in mock mode (e.g. http://localhost:5001 for Docker or local API).
VITE_API_BASE=http://localhost:5001

# Optional: Socket.IO realtime server for live board updates
# VITE_REALTIME_URL=http://localhost:3001
