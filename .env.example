# Docker Compose environment variables (optional)
# Copy to .env and customize as needed.
#
# Realtime: disabled by default. To enable live board updates via Docker:
# 1. Uncomment and set REALTIME_SERVER_URL below
# 2. Run: docker compose --profile realtime up --build
# 3. Set VITE_REALTIME_ENABLED=true and VITE_REALTIME_URL=http://localhost:3001 in Frontend/.env
# REALTIME_SERVER_URL=http://realtime:3001
